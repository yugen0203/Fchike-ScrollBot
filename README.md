# Fチケ自動スクロール プロジェクト

北海道日本ハムファイターズ「Fチケ（ticket.fighters.co.jp）」マイシーズンシートを、
最新試合（最下部の出品ボタン位置）まで自動でスクロールするための仕組み。

開発は2フェーズ：

- **Phase 1** … ブラウザのコンソールに貼るだけのスニペット（手動運用）
- **Phase 1.1** … 一般事務スタッフがダブルクリックで使えるデスクトップアプリ「ScrollBot」

---

## Phase 1：コンソール用スニペット（手動）

詳細は `task.md` と `snippets/` を参照。

| ファイル | 役割 |
|----------|------|
| `snippets/01_myseat-fetch-all.{md,txt}` | API一括取得（全48試合データを約15秒で取得・データのみ） |
| `snippets/02_myseat-autoscroll.{md,txt}` | 画面を最下部(9/24)まで自動スクロール（約17秒） |
| `マイシーズンシート_スクロール再現.mp4` | スクロール動作の再現動画 |

**使い方**：Fチケのマイシーズンシートを開き（ログイン済み）、Chrome の検証ツール →
Console に `snippets/02_…txt` の中身を貼り付けて Enter。
（貼り付けがブロックされたら一度 `allow pasting` と入力 → 再度貼り付け）

**判明した仕組み**：遅延読み込みは `pack/myseat?offset=N&limit=10` のページングAPI。
認証は localStorage の Cognito idToken（`Authorization: Bearer`）。

---

## Phase 1.1：ScrollBot（デスクトップアプリ）

### 何ができる
ログイン → マイシーズンシートへ遷移 → **最下部まで自動スクロールして停止**
（最新試合の「出品」ボタンが見える状態）。Python/Chrome/ターミナル不要。

### 利用者向け（配布後）
1. 配布フォルダ内の **ScrollBot.app（Mac） / ScrollBot.exe（Win）** をダブルクリック
   - ※Mac初回は「右クリック →  開く」（未署名のため）
2. 初回のみ `設定` で **ログインID（お客様コード）／パスワード** を入力して保存
3. **開くタブ数（1〜20）** を選ぶ／必要なら「**タブを同時並行でスクロール**」をオン（既定オン）
4. `実行` を押す → ブラウザが開き、自動でログイン → 指定タブ数ぶん最下部までスクロール
   - ログインは最初の1回のみ（同一ブラウザ＝セッション共有）
   - **並行オン**: 全タブを開いて同時にスクロール（速い）／**オフ**: 1タブずつ順次
5. **処理後もブラウザは開いたまま**（最新試合の出品ボタンを確認・操作できる）。
   ブラウザは**独立プロセス**なので、**このScrollBotアプリを閉じてもブラウザは残る**。
   不要になったらブラウザのウィンドウを自分で閉じる
6. **ログアウト**ボタン: ブラウザを閉じてログイン情報を消去（次回[実行]時にログインからやり直し → ログインのテストに使える）
7. うまくいかない時は `ログ確認`

> 仕組み: 同梱Chromium をリモートデバッグ(CDP)付きの独立プロセスとして起動し、操作後は接続だけ切る。
> ログイン状態は Chromium の永続プロファイル（`<データ領域>/chrome_profile`）に保存。
> 既定値は `config.yml` の `tabs:` / `parallel:` / `keep_open:` / `debug_port:` で変更可（GUIの選択が優先）。

### 開発者向け
- 実行: `python -m app.main`（詳細・ビルドは `README_BUILD.md`）
- 設定: サイト固有情報は **`config.yml`**（URL/セレクタ/スクロール量）。コード修正なしで別サイト転用可
- 認証情報: **`.env`**（平文・要件通り）／ ログインセッション: `profiles/<name>/storage_state.json`
- ログ: `logs/YYYY-MM-DD.log`
- **データ保存先**: 開発時はプロジェクト直下。**配布時は書き込み可能なユーザー領域**
  （mac: `~/Library/Application Support/ScrollBot/`、Win: `%APPDATA%\ScrollBot\`）。
  ※ macOS の App Translocation でアプリ本体が読み取り専用になるため、`.env`/`profiles`/`logs`/`config.yml`
  はここに保存する（同梱Chromium はアプリ本体内・読み取りのみ）。診断: `ScrollBot --paths`

### 構成
```
app/
  main.py          エントリ（GUI起動 / --selftest）
  gui.py           Tkinter GUI（実行/設定/ログ確認・ステータス）
  core.py          Playwright制御（ログイン/遷移/汎用スクロール/セッション保存）
  config_loader.py config.yml 読み込み・検証
  credentials.py   .env 読み書き
  logger.py        ログ出力
  paths.py         開発/同梱のパス解決・同梱Chromium指定
config.yml         サイト定義（既定: Fチケ）
build/             PyInstaller spec・ビルドスクリプト・同梱Chromium
```

### スクロール完了判定（Phase 1 からの改良）
固定px/固定待機ではなく、**`scrollHeight` が連続 N 回不変なら最下部**と判定（遅延読み込み対応）。
末端到達時は追加待機（`settle_ms`）でAPI完了を待つ。特定日付のハードコードは持たない（サイト非依存）。

### 検証済み（2026-05-31）
- 開発実行で GUI 起動・設定保存（.env）・最下部(9/24)到達
- **パッケージ版（.app / onedir）をクリーン環境（Python無し）で実行 → 同梱Chromiumで最下部到達（`SELFTEST: OK`）**
- 実ログイン（ID/PW入力）の最終確認は、利用者が本物の認証情報を入力して実施

### Phase 1.2 以降（未実装）
複数アカウント切替 / スケジュール実行 / プロキシ・VPN / 出品ボタン自動クリック /
ランダム待機 / 自動更新 / 認証情報の暗号化 / Windows実機ビルド・コード署名・公証。

---

## Phase 2：出品Bot（ListBot）＝ ScrollBot とは別アプリ

> ⚠️ **このアプリは実際にチケットを「出品（販売登録）」します。** 既定はフル自動（最終「出品する」まで）です。
> テスト時は必ず**確認モード（最終ボタン直前で停止）**を使うか、出品後に**手動でキャンセル**してください。

### 位置づけ
- **ScrollBot（Phase 1.1）はそのまま残す。** Phase 2 は**同一リポジトリ内の別アプリ** `listbot/` として新規作成し、
  ScrollBot の基盤（ブラウザ起動・ログイン・スクロール・パス解決・設定/認証/ログ）を**流用**する。
- 配布物も別（`出品Bot.app` / `ListBot.exe`）。データ保存先は別領域（`ListBot/`）で、ログインも独立。

### 目的
シーズンシートの自分の座席を、**指定した日付範囲の試合**について、
**販売可能価格の最高額**で**自動出品**する。連番席は設定したルールでグルーピングして出品する。

### 出品フロー（画面収録 2026-05-31 19.44.15 で確認済み）
対象サイト: `https://ticket.fighters.co.jp`（Fチケ／シーズンシート、ログイン済み）

1. ログイン → シーズンページ → **最下部まで自動スクロール**（ScrollBot のスクロールを流用し、対象日付の試合カードを表示）
2. 対象日付の**試合カード**を開く → 座席カードの **「出品する」** ボタンを押す → 出品登録ページへ遷移
3. **STEP1（価格設定）**：各座席行の**鉛筆（編集）アイコン**を押し、
   表示される価格範囲（例 `¥3,600〜¥6,900`）の**最高額（¥6,900）**を入力 → 確定 → **「次へ」**
4. **STEP2（出品方法）**：`バラ売り不可`（連番＝まとめ売り）または `バラ売り可`（単独売り）を選択 → **「次へ」**
5. **STEP3（確認）**：販売価格合計・振込予定金額を確認 → **「出品する」** → **出品完了**

### 価格ルール
- 各座席の表示価格範囲 `¥min〜¥max` の **`max`（最高額）** を自動入力する（要件：必ず最高額で出品）。
- 複数座席で max が異なる場合は**座席ごとに**その座席の max を入力する。

### 連番グルーピング（GUIで設定可能）
- **既定**：連続した席は**まとめて1グループ**で連番出品（`バラ売り不可`）。
  - 例：`5番・6番` → **2連番**（1グループ）／ `5・6・7・8番` → **4連番**（1グループ）。
- **ケース別の分割も設定できる**（ユーザーが自分で設定）。代表的な分割ポリシー：
  - `まとめる`（既定）：連続Nをそのまま1グループ（N連番）。
  - `最大グループサイズで分割`：例 最大2なら `4→2+2`、`5→2+2+1`、`6→2+2+2`。
  - `端数の扱い`：余り1席を「単独出品（バラ売り可）」にするか「直前グループに足して3連番化」するか選択。
- **最大6〜8連番**まで起こり得るため、特定数のハードコードはせず**汎用的な分割ルール**として実装する。
- **非連続席・単独席**は `バラ売り可` で単独出品する。
- グループ単位＝1出品トランザクション（同一グループ内の席を選択し、バラ売り不可でまとめて出品）。

### 日付の選択（セットアップ）
- 最初に開く画面（出品セットアップ）で、**「何月何日〜何月何日」**の野球の試合を出品対象として選択する。
- 実行すると、自動ログイン → 最下部までスクロール → 対象期間内の各試合を順に/並行で出品する。

### 並行処理（複数日付の同時出品）
- 方式：**1ブラウザ・複数タブ/ウィンドウ（ログイン共有）**。
- 対象期間の各日付を**タブ（またはウィンドウ）に振り分けて同時処理**する。
  - 例：`9/17〜9/20` を出品 → タブ1=9/17、タブ2=9/18、タブ3=9/20 …を並行で出品。
- ログインは最初の1回のみ（同一ブラウザのセッションを全タブで共有）。
- 既定の並行数・タブ割り当ては GUI / `config_listbot.yml` で調整可。

### 実行モード（安全装置）
- **フル自動（既定）**：最終「出品する」まで自動で実行し、出品を確定する。
- **確認モード（トグル）**：STEP3 の確認画面（最終「出品する」ボタンの**直前**）で停止し、人が最終確認して押す。
  - 誤出品防止・テスト・セレクタ検証に使用する。
- ⚠️ テスト運用：**9/17 18:00 の試合**は出品テストに使用可。
  出品が確定したら**必ず手動でキャンセル**する（または開発者がユーザーに手動キャンセルを依頼する）。

### 設定駆動（コードにハードコードしない）
- 出品フローのURL・セレクタ・手順・価格/連番ルールは `config_listbot.yml`（または既存 config の拡張）に集約。
  ScrollBot 同様、サイト固有値はコードに直書きせず、別サイト/仕様変更に追従しやすくする。

### 構成（予定）
```
listbot/
  main.py          エントリ（GUI起動 / --selftest / --paths）
  gui.py           出品セットアップGUI（日付範囲・連番ルール・並行数・実行モード）
  listing.py       出品本体（試合カード→出品する→価格最高額→バラ売り→確認→出品）
  grouping.py      連番グルーピング（汎用・設定可能）
  （paths/config_loader/credentials/logger/browser_launcher/scroll は app/ を流用 or コピー）
config_listbot.yml  出品サイト定義（フロー手順・セレクタ・既定の連番/価格/並行ルール）
```

### 構成（実装済み）
```
listbot/
  main.py          エントリ（GUI / --paths / --set-cred / --group / --capture）
  gui.py           出品セットアップGUI（日付範囲・連番ルール・並行数・確認モード）
  runner.py        複数日付の並行オーケストレーション（1ブラウザ複数タブ）
  listing.py       出品本体（カード展開→出品する→最高額→バラ売り→確認→出品）
  grouping.py      連番グルーピング（設定可・単体テスト済み）
  pricing.py       価格範囲→最高額の解析
  core.py          ログイン＋最下部スクロール（ScrollBot 流用）
  paths/config_loader/credentials/logger/browser_launcher  基盤（ScrollBot 流用・独立データ領域）
config_listbot.yml  サイト定義（出品フローのセレクタ・既定の連番/価格/並行ルール）
```

### 実DOM（2026-05-31 捕捉済み・config_listbot.yml に反映）
出品登録は**1ページで順次展開**: STEP1価格 →「次へ」→ STEP2バラ売り →「次へ」→ STEP3確認 →「出品する」。
- 操作ボタンは常に `#PageResaleRegisterTranslateBtn`（ラベルが 次へ→出品する に変化、最終は赤 `_danger`）。
- 価格は `input.UIPriceEditor_Right_Input` に入力後 **Tab(blur) で確定**しないと「次へ」が有効化されない。
- 座席チェック `button.UIResaleTicketCheckBox_Check`（`_checked`）、価格範囲 `.UIPriceEditor_Left_Note`。

### 検証済み（2026-05-31〜06-01）
- 連番グルーピング/最高額/日付解析の単体テスト 全PASS（`python -m listbot.test_grouping`）。
- 確認モードで 9/17 全フロー（5・6番→[2連番]→¥6,900→バラ売り不可→STEP3）動作。
- **本番フロー：実際に「出品する」→ 出品完了まで成功**（その後ユーザーが手動キャンセル）。
- **並行2タブ（6/12 & 9/17 同時）動作**。6/12は6席を区画・列ごとに `[5,6][8,9][5,6]` の3グループへ正しく分割。座席ごとの最高額（¥8,400 / ¥6,900）を個別検出。
- **パッケージ版（onedir / 出品Bot.app）をビルド・起動確認**（同梱Chromium・独立データ領域 `~/Library/Application Support/ListBot/`）。

### ビルド（配布物）
- mac: `bash build/make_listbot_mac.sh` → `dist/ListBot/`（onedir）/ `dist/出品Bot.app`
- Windows: GitHub Actions `windows-build.yml` の `build-listbot-windows` ジョブ → `ListBot-Windows.zip`
- ScrollBot のビルドとは独立（dist を共有・互いに上書きしない）。

### 使い方（配布後）
1. `出品Bot.app`（mac・初回は右クリック→開く）/ `ListBot.exe`（Win）を起動
2. 初回 `設定` で ログインID／パスワードを保存
3. 出品する**試合の日付範囲**（例 9/17〜9/20）、**連番ルール**、**並行タブ数**、**確認モード**を選ぶ
4. `出品開始` → 自動ログイン → 最下部までスクロール → 対象日付を（並行で）最高額出品
   - ⚠️ 既定はフル自動（実際に出品確定）。テストは**確認モードON**（最終ボタン直前で停止）を推奨。
